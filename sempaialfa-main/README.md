# Alfa-AI: Бизнес-ассистент с искусственным интеллектом

Alfa-AI - это веб-приложение на Django, представляющее собой интеллектуального бизнес-ассистента с поддержкой AI-моделей через OpenRouter API.

## 🚀 Основные возможности

- **AI-ассистент для бизнеса**: Интеллектуальный помощник для управления бизнес-задачами
- **Управление календарем**: Создание, редактирование и удаление событий через естественный язык
- **Анализ финансов**: Работа с чеками, балансами счетов, налогами и коммунальными услугами
- **Управление документами**: Загрузка и анализ документов (PDF, DOCX, XLSX, TXT, изображения)
- **Инвентаризация**: Управление товарами и складскими остатками
- **Управление сотрудниками**: Просмотр и анализ данных о сотрудниках
- **Экспорт данных**: Экспорт истории чата в форматах DOCX, CSV, PNG
- **Адаптивный дизайн**: Полностью адаптивный интерфейс для мобильных устройств и десктопов
- **Темная тема**: Поддержка темной и светлой темы оформления

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки без Docker)
- PostgreSQL 15+ (если не используется Docker)

## 🛠️ Установка и запуск

### Вариант 1: Запуск через Docker Compose (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd sempaialfa-main
   ```

2. **Создайте файл `.env` на основе примера:**
   ```bash
   cp env.example .env
   ```

3. **Настройте переменные окружения в `.env`:**
   ```env
   # База данных PostgreSQL
   DB_ENGINE=django.db.backends.postgresql
   DB_NAME=alfa_db
   DB_USER=alfa_user
   DB_PASSWORD=alfa_password
   DB_HOST=db
   DB_PORT=5432

   # Django настройки
   SECRET_KEY=your-secret-key-here-change-in-production
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1,130.193.35.191

   # OpenRouter настройки
   OPENROUTER_API_KEY=your-openrouter-api-key-here
   OPENROUTER_MODEL=openai/gpt-oss-20b:free
   ```

4. **Получите API ключ OpenRouter:**
   - Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
   - Перейдите в раздел [API Keys](https://openrouter.ai/keys)
   - Создайте новый API ключ
   - Вставьте ключ в переменную `OPENROUTER_API_KEY` в файле `.env`

5. **Запустите приложение:**
   ```bash
   docker-compose up --build
   ```

6. **Откройте в браузере:**
   ```
   http://localhost:8000
   ```

### Вариант 2: Локальный запуск (без Docker)

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настройте базу данных PostgreSQL** или используйте SQLite для разработки

3. **Создайте файл `.env`** (см. выше)

4. **Выполните миграции:**
   ```bash
   cd aichat
   python manage.py migrate
   ```

5. **Создайте суперпользователя (опционально):**
   ```bash
   python manage.py createsuperuser
   ```

6. **Запустите сервер:**
   ```bash
   python manage.py runserver
   ```

## ⚙️ Настройка AI-модели

Проект использует **OpenRouter API** для работы с AI-моделями. OpenRouter предоставляет доступ к множеству языковых моделей через единый API.

### Модель по умолчанию

По умолчанию используется бесплатная модель: `openai/gpt-oss-20b:free`

### Выбор другой модели

Вы можете выбрать любую модель из каталога OpenRouter:
- Перейдите на [OpenRouter.ai/models](https://openrouter.ai/models)
- Найдите подходящую модель
- Укажите её ID в переменной `OPENROUTER_MODEL` в файле `.env`

Примеры других моделей:
- `deepseek/deepseek-r1-distill-llama-8b` - Дистиллированная версия DeepSeek-R1
- `meta-llama/llama-3.1-8b-instruct` - Meta Llama 3.1
- `qwen/qwen-2.5-7b-instruct` - Qwen 2.5

### Получение API ключа

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Перейдите в [API Keys](https://openrouter.ai/keys)
3. Создайте новый ключ
4. Добавьте ключ в `.env` файл:
   ```env
   OPENROUTER_API_KEY=sk-or-v1-your-key-here
   ```

## 📁 Структура проекта

```
sempaialfa-main/
├── aichat/                  # Основное приложение Django
│   ├── main/                # Главное приложение
│   │   ├── views.py         # Обработка запросов к AI
│   │   ├── models.py        # Модели данных
│   │   └── ...
│   ├── static/              # Статические файлы
│   │   ├── css/             # Стили
│   │   ├── js/              # JavaScript
│   │   └── images/          # Изображения и иконки
│   ├── templates/           # HTML шаблоны
│   └── settings.py          # Настройки Django
├── docker-compose.yml       # Конфигурация Docker
├── Dockerfile               # Образ Docker
├── requirements.txt         # Python зависимости
├── env.example              # Пример файла переменных окружения
└── README.md                # Этот файл
```

## 🔐 Переменные окружения

Основные переменные окружения (см. `env.example`):

| Переменная | Описание | Пример |
|------------|----------|--------|
| `OPENROUTER_API_KEY` | API ключ OpenRouter | `sk-or-v1-...` |
| `OPENROUTER_MODEL` | ID модели AI | `openai/gpt-oss-20b:free` |
| `DB_NAME` | Имя базы данных | `alfa_db` |
| `DB_USER` | Пользователь БД | `alfa_user` |
| `DB_PASSWORD` | Пароль БД | `alfa_password` |
| `SECRET_KEY` | Секретный ключ Django | Сгенерируйте новый |
| `DEBUG` | Режим отладки | `True` / `False` |
| `ALLOWED_HOSTS` | Разрешенные хосты | `localhost,127.0.0.1` |

## 🧪 Тестирование

Для тестирования API используйте:
- `OPENAPI.md` - Полная документация API
- `OPENAPI_QUICKSTART.md` - Быстрый старт с API
- `test_data.json` - Тестовые данные

## 📚 Дополнительная документация

- `CHANGELOG.md` - История изменений
- `ADMIN_PANEL.md` - Работа с админ-панелью
- `METRICS.md` - Метрики и аналитика
- `TESTING.md` - Руководство по тестированию
- `DOCKER_DATABASE.md` - Настройка базы данных в Docker

## 👥 Команда разработки

- **Тасуев Абдуллах** - Продуктовый аналитик
- **Скоринов Антон** - UX/UI-дизайнер
- **Николаева Анастасия** - Fullstack разработчик

## 📝 Лицензия

Проект разработан в рамках задания от хакатона альфа-будущее.

## 🐛 Решение проблем

### Ошибка подключения к OpenRouter

Если вы видите ошибку `429 Too Many Requests`:
- Проверьте баланс на аккаунте OpenRouter
- Убедитесь, что используете бесплатную модель (если не пополняли баланс)
- Подождите несколько секунд и повторите запрос

### Ошибка `404: No endpoints found`

- Проверьте правильность названия модели в `OPENROUTER_MODEL`
- Убедитесь, что модель доступна на [OpenRouter.ai/models](https://openrouter.ai/models)
- Убедитесь, что API ключ правильно указан в `.env`

### Проблемы с базой данных

- Проверьте, что PostgreSQL запущен (если используете локально)
- Проверьте переменные окружения для подключения к БД
- Для Docker: убедитесь, что контейнер `alfa-ai-db` запущен

## 🔗 Полезные ссылки

- [OpenRouter.ai](https://openrouter.ai/) - Платформа для доступа к AI-моделям
- [OpenRouter Models](https://openrouter.ai/models) - Каталог доступных моделей
- [Django Documentation](https://docs.djangoproject.com/) - Документация Django
- [Docker Documentation](https://docs.docker.com/) - Документация Docker

---

**Версия:** 2.1 
**Последнее обновление:** 04.12.25

