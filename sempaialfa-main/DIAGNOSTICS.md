# Диагностика проблемы с AI помощником

## Проблема
AI помощник показывает только "Думаю..." и не получает ответы.

## Решения, которые были внесены

### 1. Добавлено детальное логирование
- Логи на бэкенде теперь показывают каждый шаг обработки запроса
- Логи на фронтенде показывают статус проверки запросов

### 2. Добавлена защита от зависших запросов
- Максимальное время ожидания: 5 минут
- Максимальное количество попыток проверки: 150 (каждые 2 секунды)
- Автоматическая обработка ошибок и таймаутов

### 3. Улучшена обработка ошибок
- Более информативные сообщения об ошибках
- Автоматическое восстановление после ошибок сети
- Обработка случаев, когда запрос не найден на сервере

## Как диагностировать проблему

### Шаг 1: Проверьте логи Django

```bash
# Если используете Docker
docker-compose logs -f web

# Или если запускаете локально
# Логи будут в консоли, где запущен сервер
```

Ищите в логах:
- `🚀 Начало обработки запроса` - запрос начал обрабатываться
- `✅ ChatRequest найден` - запрос найден в базе данных
- `🔑 OpenRouter настройки` - проверьте, установлен ли API ключ
- `📤 Отправка запроса в OpenRouter` - запрос отправлен
- `📥 Получен ответ от OpenRouter` - ответ получен
- `✅ Успешный ответ от OpenRouter (200)` - ответ успешен
- `💾 Сохранение результата` - результат сохранен

Если видите ошибки:
- `❌ API ключ OpenRouter не настроен!` - нужно установить `OPENROUTER_API_KEY` в `.env`
- `❌ Ошибка при отправке запроса в OpenRouter` - проблема с подключением к OpenRouter
- `❌ КРИТИЧЕСКАЯ ОШИБКА` - общая ошибка обработки

### Шаг 2: Проверьте консоль браузера

Откройте DevTools (F12) → вкладка Console

Ищите:
- `🔍 Проверка статуса запроса` - проверка статуса работает
- `📊 Статус запроса: processing/pending/completed/failed` - текущий статус
- `✅ Ответ получен` - ответ успешно получен
- `❌ Ошибка` - какая-то ошибка произошла

### Шаг 3: Проверьте Network tab в DevTools

1. Откройте DevTools (F12) → вкладка Network
2. Отправьте сообщение AI помощнику
3. Проверьте запросы:
   - `POST /api/chat/` - должен вернуть `200 OK` с `request_id`
   - `GET /api/chat-status/{request_id}/` - должен периодически вызываться и возвращать статус

### Шаг 4: Проверьте настройки OpenRouter

Убедитесь, что в файле `.env` установлен правильный API ключ:

```env
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_MODEL=deepseek/deepseek-r1
```

Проверить можно через админ-панель Django:
1. Откройте `http://localhost:8000/admin/`
2. Перейдите в раздел "Chat Requests"
3. Найдите последний запрос и проверьте его статус и ошибки

### Шаг 5: Проверьте базу данных

Если используете Django shell:

```bash
# Docker
docker-compose exec web python manage.py shell

# Локально
cd aichat
python manage.py shell
```

В shell выполните:

```python
from main.models import ChatRequest
from django.utils import timezone
from datetime import timedelta

# Последние 10 запросов
recent = ChatRequest.objects.order_by('-created_at')[:10]
for req in recent:
    print(f"ID: {req.id}, Status: {req.status}, Created: {req.created_at}, Error: {req.error}")

# Запросы, которые зависли (обрабатываются более 5 минут)
stuck = ChatRequest.objects.filter(
    status__in=['processing', 'pending'],
    created_at__lt=timezone.now() - timedelta(minutes=5)
)
print(f"\nЗависших запросов: {stuck.count()}")
for req in stuck:
    print(f"ID: {req.id}, Status: {req.status}, Created: {req.created_at}")
```

## Типичные проблемы и решения

### Проблема: API ключ не установлен
**Решение:** Установите `OPENROUTER_API_KEY` в файле `.env` и перезапустите сервер

### Проблема: Запросы зависают на "processing"
**Возможные причины:**
1. OpenRouter API не отвечает (проверьте интернет-соединение)
2. Модель недоступна (попробуйте другую модель в `OPENROUTER_MODEL`)
3. Ошибка в коде обработки (проверьте логи на наличие исключений)

**Решение:** 
- Проверьте логи на наличие ошибок
- Попробуйте другую модель: `OPENROUTER_MODEL=openai/gpt-oss-20b:free`
- Проверьте баланс на OpenRouter (если используется платная модель)

### Проблема: Запросы не создаются
**Решение:**
- Проверьте, что запрос `POST /api/chat/` возвращает `200 OK`
- Проверьте консоль браузера на наличие ошибок JavaScript
- Проверьте, что файл `chat.html` загружается без ошибок

### Проблема: Статус не обновляется
**Решение:**
- Проверьте, что запросы `GET /api/chat-status/{request_id}/` выполняются
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что `request_id` сохраняется в сообщении

## Дополнительная информация

После внесенных изменений:
- Все запросы логируются с эмодзи для удобства поиска
- Зависшие запросы автоматически обрабатываются через 5 минут
- Ошибки более информативны и помогают быстро найти проблему

Если проблема не решена, соберите следующую информацию:
1. Логи Django (последние 50-100 строк)
2. Скриншот консоли браузера
3. Скриншот Network tab с запросами
4. Статус последних 5 запросов из базы данных

