# Документация по админ-панели

## Обзор

Админ-панель Django предоставляет полнофункциональный интерфейс для управления всеми данными проекта Альфа-Ассистент.

## Доступ к админ-панели

1. Создайте суперпользователя (если еще не создан):
```bash
cd sempaialfa-main/aichat
python manage.py createsuperuser
```

2. Запустите сервер:
```bash
python manage.py runserver
```

3. Откройте админ-панель в браузере:
```
http://localhost:8000/admin/
```

4. Войдите с учетными данными суперпользователя

## Модели в админ-панели

### 1. ChatRequest (Запросы к AI)

**Расположение:** `/admin/main/chatrequest/`

**Возможности:**
- Просмотр всех запросов к AI
- Фильтрация по статусу и дате
- Поиск по ID и тексту сообщения
- Детальный просмотр каждого запроса
- Просмотр метрик запроса (inline)

**Отображаемые поля в списке:**
- ID (короткий)
- Статус (с цветными бейджами)
- Превью сообщения
- Наличие ответа
- Наличие действия
- Дата создания
- Время обработки

**Детальный просмотр включает:**
- Полный текст запроса и ответа
- JSON данные (пользователь, файлы, история)
- Действия AI
- Ошибки (если есть)
- Метрики запроса (inline)

**Фильтры:**
- По статусу (pending, processing, completed, failed)
- По дате создания
- По дате обновления

### 2. ChatHistory (История чатов)

**Расположение:** `/admin/main/chathistory/`

**Возможности:**
- Просмотр всех чатов
- Фильтрация по дате
- Поиск по chat_id, email, заголовку
- Просмотр сообщений в чате
- Просмотр действий AI

**Отображаемые поля в списке:**
- Chat ID (короткий)
- Email пользователя
- Заголовок чата
- Количество сообщений (с бейджем)
- Количество действий
- Дата последнего сообщения
- Дата создания

**Детальный просмотр включает:**
- Статистика чата
- Все сообщения (с форматированием)
- Действия AI (JSON)
- Метаданные

**Фильтры:**
- По дате создания
- По дате последнего сообщения
- По количеству сообщений

### 3. Metric (Метрики)

**Расположение:** `/admin/main/metric/`

**Возможности:**
- Просмотр всех метрик качества
- Фильтрация по категории и дате
- Поиск по названию метрики
- Отслеживание достижения целей

**Отображаемые поля в списке:**
- Название метрики
- Категория (с цветным бейджем)
- Текущее значение
- Целевое значение
- Единица измерения
- Статус достижения цели (✓/✗)
- Размер выборки
- Дата расчета

**Детальный просмотр включает:**
- Визуальное отображение значений
- Индикатор статуса (достигнута ли цель)
- Период расчета
- Метаданные (JSON)

**Категории метрик:**
- Качество ответов (синий)
- Выполнение действий (зеленый)
- Производительность (оранжевый)
- Надежность (фиолетовый)
- Безопасность (красный)
- Пользовательский опыт (голубой)
- Обработка файлов (коричневый)
- Точность данных (серо-синий)
- Работа с календарем (розовый)
- Форматирование (серый)

**Фильтры:**
- По категории
- По дате расчета
- По названию метрики
- По единице измерения

### 4. ChatRequestMetrics (Метрики запросов)

**Расположение:** `/admin/main/chatrequestmetrics/`

**Возможности:**
- Просмотр метрик для каждого запроса
- Фильтрация по различным параметрам
- Ссылки на связанные запросы

**Отображаемые поля в списке:**
- Ссылка на запрос
- Время обработки (с цветовой индикацией)
- Время обработки LLM
- Наличие действия (бейдж)
- Успешность действия (бейдж)
- Информация о файлах
- Дата создания

**Детальный просмотр включает:**
- Производительность (временные метрики)
- Качество (действия, контекст, длина ответа)
- Обработка файлов (статистика)
- Модерация (блокировки)
- Использование контекста (визуальный индикатор)
- Метаданные (JSON)

**Фильтры:**
- По наличию действия
- По успешности действия
- По блокировке сообщения
- По блокировке ответа
- По использованию контекста
- По наличию файлов
- По дате создания

## Особенности интерфейса

### Цветовая индикация

**Статусы запросов:**
- 🟠 Ожидает обработки (pending)
- 🔵 Обрабатывается (processing)
- 🟢 Завершено (completed)
- 🔴 Ошибка (failed)

**Время обработки:**
- 🟢 < 5 секунд (отлично)
- 🟠 5-10 секунд (нормально)
- 🔴 > 10 секунд (медленно)

**Достижение целей:**
- 🟢 ✓ ДА - цель достигнута
- 🔴 ✗ НЕТ - цель не достигнута
- ⚪ N/A - цель не задана

### Группировка полей (Fieldsets)

Все модели используют fieldsets для удобной группировки полей:
- Основная информация
- Детальные данные
- Метаданные (сворачиваемые секции)

### Inline редактирование

В ChatRequest доступно inline редактирование метрик:
- Метрики отображаются прямо в форме запроса
- Все поля метрик доступны для просмотра
- Автоматическая связь между запросом и метриками

### JSON отображение

Все JSON поля отображаются в удобочитаемом формате:
- Форматированный JSON с отступами
- Прокручиваемые области для больших данных
- Подсветка синтаксиса

## Действия (Actions)

В будущем можно добавить массовые действия:
- Пересчет метрик для выбранных запросов
- Экспорт данных
- Очистка старых запросов

## Поиск и фильтрация

### Поиск
- ChatRequest: по ID и тексту сообщения
- ChatHistory: по chat_id, email, заголовку
- Metric: по названию и категории
- ChatRequestMetrics: по ID запроса

### Фильтры
Все модели имеют расширенные фильтры по:
- Датам (с иерархией дат)
- Статусам
- Булевым полям
- Категориям

## Экспорт данных

Для экспорта данных можно использовать:
1. Django admin actions (можно добавить кастомные)
2. API endpoints (`/api/metrics/`, `/api/chat-history/`)
3. Прямой доступ к базе данных

## Настройка языка

Админ-панель настроена на русский язык (`LANGUAGE_CODE = 'ru-ru'`).

Для изменения языка отредактируйте `aichat/settings.py`.

## Безопасность

- Доступ только для суперпользователей
- Все важные поля только для чтения (readonly)
- Валидация данных на уровне модели
- Защита от CSRF атак (встроено в Django)

## Расширение админ-панели

Для добавления новых функций:

1. **Кастомные действия:**
```python
@admin.action(description='Пересчитать метрики')
def recalculate_metrics(modeladmin, request, queryset):
    # Ваш код
```

2. **Кастомные фильтры:**
```python
class CustomFilter(admin.SimpleListFilter):
    # Ваш код
```

3. **Кастомные виджеты:**
```python
class CustomWidget(admin.widgets.AdminTextareaWidget):
    # Ваш код
```

## Полезные ссылки

- Django Admin документация: https://docs.djangoproject.com/en/4.2/ref/contrib/admin/
- Кастомизация админки: https://docs.djangoproject.com/en/4.2/ref/contrib/admin/customization/

